import React, { useState, useEffect,useContext } from 'react';
import { UserContextDecode } from './DecodeImage';

// const userImage = useContext(UserContextDecode);

const TextDetection = () => {
    
};

export default TextDetection;
